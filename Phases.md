# Phases.md

## Phase 0 — Discovery
Audit sandbox, Agent Forge skills, assessment PDF, and host constraints.

## Phase 1 — Foundation
Design tokens, docs, database schema, folder layout.

## Phase 2 — Python scraper
RSS ingest, normalization, body extract, TF-IDF clustering, JSON output.

## Phase 3 — Node ingest + API
TypeScript port of the pipeline, Postgres store, required REST endpoints, job polling.

## Phase 4 — Timeline UI
Masthead, time-axis visualization, cluster detail, source filter, refresh, auto-refresh.

## Phase 5 — Site pages
About/method, privacy, terms, 404, SEO files.

## Phase 6 — Accessibility + mobile
Skip link, keyboard, reduced motion, 390px layout, overflow.

## Phase 7 — QA (COMPLETE)
Typecheck PASS, build PASS, lint PASS, browser smoke PASS (dev + mobile), ingest runtime PASS, API status codes PASS.

## Phase 8 — Release review (COMPLETE)
All requirements verified. Project is READY FOR SUBMISSION.
